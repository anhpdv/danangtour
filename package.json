{
  "name": "danangtour",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "crawl": "node scripts/crawl.mjs",
    "import:cms": "npm run import:cms --prefix web",
    "seed:admin": "npm run seed:admin --prefix web"
  }
}
